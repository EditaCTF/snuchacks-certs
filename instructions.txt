===============================================
  SNUC HACKS CERTIFICATE SIGNER
===============================================

    pip install -r requirements.txt
    python sign.py -i pdf -o signed -p certificate.p12 -s PASSWORD -f sign.png

Stamp position: edit `SIG_BOX` at the top of sign.py (PDF points, y=0 = bottom).

Two-signatory workflow (Faculty A → Faculty B with separate stamps + PKCS#7):
see the `two-stage-signer` branch.

Cert PDFs: https://ssneduin-my.sharepoint.com/:f:/g/personal/vijayan23110015_snuchennai_edu_in/IgCo3vYF6x7qTb_MjPGE2BeHAdNVTKnET5ejZ4VGMAZgqUk?e=D9BRde